```plantuml
@startuml
title UseCase7 - View Scores

actor Player

participant MainMenu
participant ResultMenu
participant Engine
participant StateMachine
participant EventListener
participant ScoreboardMenu

alt From Main Menu
    Player -> MainMenu : Click "Scoreboard"
    MainMenu -> Engine : handleTrigger()

else From Result Menu
    Player -> ResultMenu : Click "Scoreboard"
    ResultMenu -> Engine : handleTrigger()
end

Engine -> StateMachine : stepState(SCORE_BOARD)
Engine -> EventListener : createEvent(STATE_TRANSITION)

Engine -> Engine : handleMenuTransitions()
Engine -> ScoreboardMenu : set current menu

@enduml
```
