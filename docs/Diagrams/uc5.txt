```plantuml
@startuml
title UseCase5 - Resume Game

actor Player

participant PauseMenu
participant Engine
participant StateMachine
participant EventListener
participant PlayingScreen

alt Resume Button
    Player -> PauseMenu : Click "Resume"

    PauseMenu -> Engine : handleTrigger()
    Engine -> StateMachine : stepState(PLAY)

else Escape Key
    Player -> Engine : Press Escape Key
    Engine -> StateMachine : stepState(PLAY)
end

Engine -> EventListener : createEvent(STATE_TRANSITION)
Engine -> Engine : handleMenuTransitions()
Engine -> PlayingScreen : show maze

@enduml
```
